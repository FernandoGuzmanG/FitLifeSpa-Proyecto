server:
  port: 0

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_fitlifespa_test
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    enabled: false